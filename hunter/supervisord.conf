[supervisord]
nodaemon=true
logfile=/data/log/supervisord.log

[program:hunter]
command=/usr/local/bin/uwsgi --ini  /etc/uwsgi.ini

[program:celery]
directory=/var/hunter
command=celery -A app.celery_cron.celery worker --concurrency=4 -l debug --log-file=/data/log/worker-%i.log
